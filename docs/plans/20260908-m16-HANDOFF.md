# M16 handoff — 2026-09-08 evening (end of day 3) — START HERE

**Where:** worktree `~/port_kokkos_sp`, branch `m16-precision` (off `m14-integrate d4a9fe0`), pushed to
`origin/m16-precision` through `f91aa86`; local commits after that are handoff/docs only. Plan
`docs/plans/20260902-m16-mixed-precision.md` (ticks current), registry `docs/PRECISION_ISLANDS.md` (promotion
log = the evidence trail), board `docs/MIXED_PRECISION_M16.md`, lessons `docs/KOKKOS_PORTING_LESSONS.md` L132.
The day-2 handoff (`20260907-m16-HANDOFF.md`) is the detailed diary; this file is the map.

## The one thing that changes everything from here
🔴 **Every CUDA build needs `-DKokkos_ENABLE_IMPL_CUDA_MALLOC_ASYNC=OFF`** (hard-wired in `build_m16.sh`, NOT yet in
`build_m14.sh` / `~/port_kokkos_int`). Kokkos' stream-ordered pool memory handed to CUDA-aware MPI corrupts halo slots:
8/15 thousand-step CORE2 legs died on 4 GPU nodes, 0/15 with the flag, and the step is 17 % faster. This was the
M14 "GPU CG NaN at 32–64 GPUs" class too. Every GPU number measured before 2026-09-08 15:00 is on the bad allocator.
Evidence table: registry entries dated 2026-09-08. UCX-side mechanism unidentified (memtype cache, CUDA IPC,
registration cache each ruled out) — a DKRZ/UCX ticket item, not a blocker.

## Binaries (`/work/ab0995/a270088/port2/m16/bin/<tag>/{dp,sp}/fesom_port_{serial,cuda}` + PROVENANCE.txt)
- **`e3` = the current pair** (`fa472ba` + flag): dp serial a6a365c5 / cuda 66ee6af6 · sp serial 08f65ea7 / cuda
  ca23f514; CUDA smoke 27313745 PASS. Use nothing older for GPU work. Serial binaries are unaffected by the flag.
- `e2scan2/dp/fesom_port_cuda` (57907ff3): the pool build WITH the device NaN scanner probes (`FESOM_MP_NANSCAN=2`)
  around the ice step — the forensic binary; `e2noasync` = fixed allocator without the extra probes.

## State by gate
| gate | status |
|---|---|
| G0 FP64 byte identity | closed: 14 configs bitwise vs the `d4a9fe0` oracle on pi and CORE2 np8; re-earned on every Serial rebuild |
| G1 SP runs CORE2 with forcing | closed (job 27288954): CG \|Δit\| vs FP64 mean 0.43 max 1 |
| G2 board | CPU rows final: fArc 4096 knobs-off **0.855**, recipe(zstar+SE90+anomaly) **0.830**; dars 8192 **0.732** / **0.731**. GPU rows: CORE2 1N on `e3` **0.870**; **CORE2 16N knobs-off/recipe and NG5 16N knobs-off/recipe RUN OVERNIGHT (jobs 27313843/44/45/46)** + staged-vs-device 16N pairs 27310270 (pool) / 27313650 (fixed). Old pool-allocator GPU rows stay in §1 as history. |
| G3 knob liveness | 13/15 live at SP (Serial + CUDA). `pipecg`/`oati` diverge in float → unusable as built; `pcsi` lives via `FESOM_SSH_FLOOR` (SP-only, announced as `floor-hits=`). Conservation 30 d: heat 0.2 %; salt drift 1.7× FP64 → 0.4 % with `FESOM_SALT_ANOMALY=1` ⇒ **the anomaly is in the SP recipe**. |
| G4 screens / 1-yr twin | fArc + dars CPU 3000-step SP screens PASS (27294513/14). GPU screens must be RE-RUN on `e3` (earlier GPU deaths were the allocator). 1-year twin not started. |

## Read first next session (the overnight jobs)
```
for j in 27310270 27313650 27313843 27313844 27313845 27313846; do echo "== $j"; grep -E '^leg |^  (dp|sp|base|best) |SP/DP|GAIN|^GCSV' /work/ab0995/a270088/port2/m14/gladder.$j.out; done
```
- 27313843/44 → board §1b CORE2 16N rows (knobs-off; recipe = EVPWIDE lean + anomaly). 27313845/46 → NG5 16N rows
  (FP64 should now SURVIVE; if it still dies, read `1_dp/run.err` for `[mp-nanscan-dev]`-free FATALs — it would be a
  different bug). For each leg also grab the step-300 `it=` for the CG column.
- 27313650 (fixed build) vs 27310270 (pool): `base` = device-pointer halo, `best` = `FESOM_HALO_STAGE=1`. ⚠️ These two
  were submitted BEFORE the ladder env-reset fix (`f91aa86`): the 4th leg (`base`) inherited `FESOM_HALO_STAGE=1` from
  the best legs — use leg 1 only for the device-path number. If staged is faster at 16N too (it was −24 % at 4N on the
  fixed build), `FESOM_HALO_STAGE=1` becomes a recipe lever / the Levante default (dolpung already mandates it).

## Critical path to "SP is usable" (in order)
1. Fill the G2 GPU rows from the overnight jobs (board §1b); if NG5 FP64 survives, the board is complete.
2. **G4 GPU screens on `e3`** (`jobs/job_m14_ladder_gpu`, `ARMS=sp NSTEPS=3000 M16_BINS=…/e3`, recipe BASE_KNOBS,
   `--exclude=l50154`): CORE2 16N, NG5 16N; plus the never-run cells fArc GPU / dars GPU (partitions exist on /pool;
   `WSPLIT=1` default) and NG5 / CORE2 CPU (compute nodes unrestricted — user 2026-09-07; CORE2 CPU is not a scaling
   run). Pass = both legs reach step 3000 finite, SE drift 0.0 where the wide halo is on, floor-hits reported.
3. **1-year CORE2 twin** SP vs FP64 at the 63A posture (2N × 4 A100-80, `e3`, restart resubmits every ~25 min,
   `-1` snapshots + monthly means, `FESOM_SALT_ANOMALY=1` in both arms), then `scripts/mp_gate4_verdict.py`
   (annual-map correlations sst/sss/ssh/a_ice + the Fortran control row). Template: `jobs/job_m16_conserv` for the
   run shape, the M14 tier1_cuda_1yr job for the resubmit chain. G4 green ⇒ the option is declared usable; the 63-yr
   decision is the user's.
4. G3 leftovers (can run in parallel on compute): true-residual rank sweep (`cg` CORE2 128→864 with
   `FESOM_SSH_VERIFY=1`, bar = CORE2 np8 true/rtol 1.1–2.1), the FP64 `FESOM_FORCING_POINTSLOPE` control leg for the
   divergence curve, the dt-seed ensemble envelope (`scripts/mp_envelope_verdict.py`).
5. Docs + merge (F1/F2): fold SP1–SP11 into the lessons (≥L133), finish `MIXED_PRECISION_M16.md` (every number with a
   job id; honest untested list), revert gate on the final tree (knobs unset + `PRECOND=0` vs ref0, Serial bitwise),
   `git merge m16-precision` into `m14-integrate` in `~/port_kokkos_int` + `jobs/job_m14_gate_serial` there.
   Carry the allocator flag into `build_m14.sh`.

## Optional / parked
- Residual replacement for `pipecg`/`oati` at SP (a day of solver work; the M14 `oati` lever on dars has no SP
  counterpart; SE covers fArc). E5.
- Upstream's namelist now ships `precond_variant = -1` (auto: 1 in SP, 0 in FP64); the port uses 1 for both (decision
  D3). A one-line change if the FP64 arm should mirror upstream's auto.
- The remaining below-float-resolution SSH residual (soltol 1e-5) — documented, upstream ships the same.

## Rules paid for this week (don't relearn)
- No model run on the login node except seconds-long pi smokes; CORE2 = SLURM; GPU ≤16 nodes without asking;
  compute nodes unrestricted for scaling (user 2026-09-07); `--exclude=l50154` (bad GPU node).
- `-Wundef` stays: an `#if MACRO` on an undefined macro reads 0 silently (the JRA point-slope bug). `scanf`-family
  `%lf` targets must be `double`. Kill by pid, never `pkill -f` with a pattern that is in your own command line.
- 300-step legs hide a ~10 %/leg failure; bisect flakes with 1000-step legs ×15 per arm and the DEVICE scanner
  (`FESOM_MP_NANSCAN=2`); the host scanner (`=1`) is blind on CUDA.
- The ladder env reset must clear every knob prefix the best arm can set (fixed `f91aa86`).

## Tools this campaign added
`scripts/m16_gate0.sh` (byte/live modes, serial/cuda env) · `jobs/job_m16_gate_{serial,cuda}` · `scripts/m16_knob_signals.sh`
· `scripts/m14_collect.py` + `scripts/m14_zombie_check.sh` (shared by both ladders) · `jobs/m16_bins.sh <tag>` ·
`jobs/job_m16_smoke_cuda` · `jobs/job_m16_core2_twins` + `scripts/m16_core2_twins_report.py` · `jobs/job_m16_conserv` +
`scripts/mp_conserv_drift.py` · `scripts/m16_salt_anomaly_gate.sh` · `scripts/m16_stiffdrift_gate.sh` ·
`scripts/m16_restart_gate.sh` · `FESOM_MP_NANSCAN=2`, `FESOM_SSH_FLOOR`, `FESOM_DIAG_STIFF_DRIFT`, `FESOM_SALT_ANOMALY`.
