# M9 HANDOFF — divergence-subcycled mEVP (sessions 1-2, 2026-08-04/05)

**Read this first, then `20260805-m9-RESULTS.md`, then the Stage-3 handoff section inside
`20260804-m9-mevp-divergence.md`.**

---

## 0. Where you are

| | |
|---|---|
| worktree | `/home/a/a270088/port_kokkos_ice` — **work here, not `~/port_kokkos`** |
| branch | `m9-mevp-double`, **22 commits, LOCAL, NOT PUSHED** (user says when) |
| base | fast-forwarded onto `main` `65a1a71` before the first commit; history linear |
| builds | `build-serial/`, `build-m9cuda/` (both current with HEAD) |
| frozen bins | `/work/ab0995/a270088/port2/m9/bin/b_<sha8>/` — **immutable per-build paths** |
| data | `/work/ab0995/a270088/port2/m9/m9_results.json` (**56 runs**), 3 figs in `…/m9/figs/` |

⚠️ `externals/kokkos` here is a **plain copy** of the pinned 4.4.01 source, not a git submodule.
`.git/modules` is shared with `~/port_kokkos`, and `git submodule update --init` would likely
repoint its `core.worktree` at this tree. Do not run it. If you need to refresh Kokkos,
`rsync -a --exclude=.git` from the parent worktree again.

## 1. Document map

| doc | what it is |
|---|---|
| `docs/plans/20260804-m9-mevp-divergence.md` | the plan. Contains the **Stage-3 handoff** (§ before "Stage 3") with the 6 remaining steps and exact line refs, plus a REV-2 correction log |
| `docs/plans/20260804-m9-PREREG.md` | pre-registration, committed `bc85f02` **before** the first fleet job |
| `docs/plans/20260805-m9-RESULTS.md` | **all results**, incl. §5b farc/Koldunov-2019 and a pre-reg scorecard |
| `docs/KOKKOS_PORTING_LESSONS.md` | **L103** diagnostic-branch-in-a-timed-kernel · **L104** launch-bound mEVP subcycle · **L105** three campaign-hygiene traps |
| `ice_sergey/` | Danilov's note + reference F90 (gitignored — public repo, not ours to redistribute) |

## 2. What exists in the code

| cell | knob | state |
|---|---|---|
| ① classic | — | baseline |
| ③ divergence | `FESOM_SPEED_MEVPDIV=1` | built, gated |
| ⑤ lagged halo | `FESOM_SPEED_MEVPLAG=K` (K divides `evp_rheol_steps`) | built, gated |
| confound control | `FESOM_SPEED_MEVPNOEPS=1` | built, gated |
| ablation | `FESOM_SPEED_MEVPDIV_NOMASK=1` | built, gated |
| diagnostic | `FESOM_MEVPDIV_SELFCHECK=1` | built (perturbs — never a timing/L3 leg) |
| ② exact wide halo, classic form | `FESOM_SPEED_EVPWIDE=K` | **BUILT, byte-certified** |
| ④ exact wide halo, divergence form | `FESOM_SPEED_EVPWIDE=K` + `MEVPDIV=1` | **BUILT, byte-certified** |

Gate ladder green: L0 byte vs C oracle (every build) · L1 null rungs (`MEVPNOEPS` **exactly**
zero) · L2 selfcheck drift 1e-15 rel · L4 CUDA fidelity · L5 options ×3 (TKE/zstar
bit-identical ⇒ knobs provably inert outside `whichEVP==1`).

Infrastructure, all worktree-rooted with `BIN=` **required**:
`jobs/job_m9_{mevp_bitid,gpu_gate,options3,ab_cpu,ab_gpu,evpwide_regress,nsys}`,
`scripts/m9_{collect,figs}.py`, `jobs/io.config.m9_timing`.
`job_m9_mevp_bitid` yields L0+L1 in one job, needs `EXPECT=<regex>` proving the knob FIRED, and
takes `CLASS=byte|solver` and `DT/NSTEPS/SNAP`.

## 3. Results in one paragraph

**Cell ⑤ — exchanging the mEVP halo every K-th subcycle — is the largest single lever this
project has measured: up to −25.9 % of the GPU model step** (CORE2 16 nodes, K=8; −13…−26 %
across the CORE2 GPU ladder), **and it reproduces on the 638 k-node production mesh**
(farc GPU −12.9 / −14.4 / −17.6 / −14.9 % at 2/4/8/16 nodes). CPU: −3.4 % (farc np2304).
It pays in proportion to the halo cost already being paid, i.e. **most at and past the
strong-scaling knee** — CORE2 GPU is past its knee at 2 nodes and degrades thereafter, farc is
flat from 4 nodes.
For scale, M7's certified levers were CGPIPE −8.07 %@16N, E.T1 env −10.3 %, h17 −2.6 %. The mechanism is measured: at np2304 the ice phase's
`MPI_Wait` falls 4.8 → 1.9 ms (−60 %) while busy barely moves (3.5 → 3.0), MPI ops/step 240 → 30,
ocean/cg untouched — it removes **idle**, which is exactly the bottleneck Koldunov et al. 2019
(GMD 12, 3991) diagnosed on this same farc mesh and whose proposed-but-untested fix ("wider
halo regions to reduce communication frequency") cells ④/⑤ implement. **Danilov's reformulation
is exact** (`R ≡ ∇·σ` to 1e-15 relative within a step; cross-step mask-flip bounded at 2.1e-11)
**but pays nothing by itself** — H1 falsified, `MEVPNOEPS` exactly 0.00 % — because the subcycle
is launch/exchange-bound (only 1.25 ms of an 8.2 ms GPU phase is kernel time over ~480
launches/step). Its value is making the **exact** wide halo affordable: one fused node message
instead of node + element-σ.

---

## 3b. SESSION 2 (2026-08-05) — P1 and P2 are DONE. Read `20260805-m9-RESULTS.md` §5 and §8.

**P1 — cell ⑤'s accuracy is now measured, and it PASSES.** 1-yr CORE2 mEVP legs, both backends,
pre-registered in `20260805-m9-PREREG-P1.md` (committed `c9a4d6a` before the first leg).
Every field clears mEVP's own C↔F floor at K ≤ 8. **But the margin is 1.1–1.5× on ice velocity,
not the order of magnitude the pre-registration predicted — P1.3 is on the record as
wrong-optimistic.** RMS *is* monotone in K (P1.4), which is the thing session 1's `max|Δ|` could
not show. The CPU arm is the arm of record: its classic replicate was **bit-identical over a
full year**, so every CPU difference is the approximation and nothing else. The GPU arm cannot
resolve it — CUDA's own replicate departs by the same order (control 2, as pre-registered).
**Recommendation: promote to a documented opt-in option at K=4.**

**P2 — cells ②/④ are BUILT and byte-certified.** Ladder green: L0, the `EVPWIDE=1` null rung,
**both cells bit-identical at K=4 and K=8** (② vs classic, ④ vs the divergence form), L4 CUDA
fidelity (worst 1.5e-3), L5 options ×3. Two defects the gate caught that inspection did not →
**L106**.

⭐ **The finding that changes the study's shape: EXACTNESS IS NOT FREE.** GPU CORE2 model step
vs classic — ⑤ lag8 **−15.3 / −20.8 / −25.5 %** at 2/8/16 nodes, against ② **+0.3 / −7.7 /
−12.5 %** and ④ **+2.0 / −8.4 / −15.3 %**. The exact cells buy roughly half to 60 % of the
approximation and *nothing* at 2 nodes, because they remove the same exchanges but add 2–3 ghost
launches per subcycle into a subcycle §4 measured to be launch-bound.

⭐⭐ **H3 answered, then confirmed on three meshes (§9.7).** Cell ④ beats ② essentially
everywhere on GPU — by 1.9–2.8 pp at K=8 and up to **7.1 pp at K=2** — and loses to it on CPU.
It halves the wide-halo message but pays one extra gather pass, so it wins wherever bytes cost
more than launches. §9.5 shows both terms directly in the phase split: ④ has *higher* busy
(13.3 vs 12.0 ms — the gather) but *lower* wait (4.2 vs 6.9 ms — half the bytes), and wait wins.
Since H1 was falsified (the reformulation buys nothing per element), **this is the only place in
the campaign where Danilov's reformulation actually pays** — and it pays exactly where aimed.

⭐⭐⭐ **The one-sentence mechanism, and it covers both backends and every mesh:** on GPU an mEVP
exchange costs mostly **launches and packing**, which the profiler charges to `busy`, not wait —
so cell ⑤ removing 105 of 120 exchanges takes `icedyn` busy from 12.1 to 3.5 ms (−71 %), while
cells ②/④ remove the *same* messages but substitute ghost kernels for them one-for-one and leave
busy at 12.0/13.3. **The approximation removes work; the exact halo only relocates it.**

## 4. What to do next, in priority order

### P0a — ~~cells ②/④ on farc at scale~~ **DONE (session 2, ~30 jobs)** — RESULTS §9.
Measured on farc, dars and NG5, both backends, K ∈ {2,4,8}. **CPU: they never win, on any mesh,
at any K** (best case is break-even at K=2, inside the ~1.5 % CPU noise floor; K=4 costs 1–9 %).
**GPU: they win where the mesh is still strong-scaling** — farc GPU is flat past 4 N so it peaks
at −6.5 % (8 N) and falls back; **dars is still scaling and reaches −9.4 % at 16 N, the best
exact-cell result of the campaign**; NG5 at 462 k nodes/rank shows nothing for any lever.
**K=2 is the WORST K for the exact cells** (opposite to cell ⑤, which wants small K on accuracy
grounds) — they improve monotonically K=2 → 4 → 8. Mechanism measured on both sides: **on GPU an
exchange costs mostly launches and packing, which the profiler charges to `busy`; cell ⑤ removes
that work, the exact halo only relocates it into ghost kernels.** Price of exactness at matched
K = **+6.6…+13.1 pp on GPU**, +0.7…+8.1 pp on CPU.

### P0b — the fused build of ② (decision D1, still open).
②'s deficit vs ④ is consistent with *both* the 2× bytes and the extra message. §9.5 now shows
the two terms separately in the phase split (④ has higher busy, lower wait), which is strong
evidence for the byte term — but **the fused build is what would settle it by construction**,
and it is the last piece of H3. Cheaper than it was: the plan is in §8's "what is NOT done".

### P1 — ~~Quantify cell ⑤'s accuracy~~ **DONE (session 2)** — see §3b and RESULTS §5.
Optional remainder: a high-resolution accuracy leg (ng5/farc). CORE2 is measured and the
recommendation is explicitly scoped to it. The original text follows as the record.

<details><summary>original P1 text</summary>

### P1 — Quantify cell ⑤'s accuracy. **This gates everything.**
`max|Δ|` at 300 steps is useless here: no monotone trend with K, every large value resting on
1–5 points of 126 858 = chaotic ice-edge divergence (K=2 shows a bigger `a_ice` outlier than
K=30). Needed: **RMS / pattern-correlation metrics** and an **L6 climate leg** at the M5.23 bar
plus hemispheric ice area/extent and drift RMS. Until this exists, ⑤ is a proven speed result
with an unquantified accuracy cost and **must not be recommended for production**.
Suggested: 1-yr CORE2 legs at K ∈ {2, 4, 8} vs classic, reusing `scripts/m7_climate_check_plots.py`
and the M5.23 bar from the CGPOLY precedent (session 13).

### P2 — ~~**BUILD CELLS ②/④**~~ **DONE (session 2)** — see §3b and RESULTS §8. Record follows.

**This is the largest unfinished piece of M9 and it is an explicit task, not an aspiration.**
Cell ④ is precisely what Koldunov et al. (2019) proposed and never tested ("wider halo regions
to reduce communication frequency"); cell ⑤ is only its cheap approximation, and ⑤'s accuracy
is unquantified (P1), so ④ is the version that could be adopted without an accuracy argument.
Cell ② is the control that says whether Danilov's reformulation is what makes ④ affordable.

**Already done and gated — do not redo:**
- **Task 11** — `evpw_exchange` takes a caller-supplied `EvpwPlan` instead of the integer `nf`.
  Regression `jobs/job_m9_evpwide_regress` (job 26698811): std-EVP EVPWIDE K=1/2/4 byte-identical
  **and** diagnostics provably alive (1200 drift lines, 20 echo lines, echo `0.000e+00`).
- **Task 12a** — every node-side mEVP array now allocates at `nw`. Three were `myDim`-extent,
  i.e. no room for ghost indices; an out-of-bounds ghost read is silent garbage on CUDA.

**Remaining, in order. Gate each before the next.**

| # | task | files | gate |
|---|---|---|---|
| 12b.1 | Dev arrays `pfac_g[Eg]`, `ice_el_g[Eg]`, `bcx[N+next]` | `fesom_ice_evpwide.{h,cpp}` (build them at `:687-693`, beside `istr_g`) | build summary prints; knob-off byte |
| 12b.2 | ship `bc_index` **once** at build — it is TIME-INVARIANT — by widening the existing owner reply from 2 doubles to 3 (`:548-589` carries `area0`,`cor`). **Do not recompute locally** (libmvec lesson) | `fesom_ice_evpwide.cpp` | ghost `bcx` == owner bytes on owned slots |
| 12b.3 | lift **BOTH** refusals — `fesom_ice_evpwide.cpp:719` **and** `fesom_ice.cpp:752-766` — **and wire `fesom_evpwide_K()` into the mEVP path**; it is currently called only from `fesom_ice_evp.cpp:611`, so without it the zone never builds while the announce claims it did | `fesom_ice_evpwide.cpp`, `fesom_ice.cpp` | announce fires under `whichEVP=1`; build summary appears |
| 13.1 | ghost per-step maps: run the owned `maevp_node_pre` / `maevp_elem_pre` over extended ranges after the prestep 11-field ship. `rhs_a/rhs_m` shipped **after** their area scaling (trap 7). Don't forget `maevp_aux_init` (spans `[0,N)`) and the coastal BC (replay via `W.coastx`, cf. `fesom_ice_evp.cpp:915`) | `fesom_ice_maevp.cpp` | ghost maps == owner values at ring 1 |
| 13.2 | ghost twin kernels: element over `[0,Eo+Eg)`, node over `[0,N+next)` mirroring `fesom_ice_evp.cpp:832-916` (owned / dist-halo / ghost-replay branches, owner-order gather `gath_ptr/gath_elem/gath_k`). ② reads ghost σ from `s11_g/s12_g/s22_g`; ④ reads ghost `R_u/R_v` from the extended Fields | `fesom_ice_maevp.cpp` | **K=1 null rung byte-identical** (with `FORCE_SERIAL` + announce grep) |
| 13.3 | window plans + cadence: ④ = `(u,v,R_u,R_v)` one segment; ② = node segment + σ element segment, built **fused and unfused** behind a flag (decision D1) | `fesom_ice_evpwide.cpp`, `fesom_ice_maevp.cpp` | prestep echo exactly `0.000e+00`; ghost-drift diagnostic present |
| 13.4 | full ladder | — | L3 at K∈{2,4,8}; L4 CUDA fidelity; L5 options ×3 |

**Contract reminder:** ring depth is **R = K**, not 2K−1 — in ④ everything carried is a node
field and everything is refreshed; in ② the carried element σ is re-baselined from the owner
every window (session-12's correction). Ring K is frozen between refreshes.

**Pre-registered expectation** (H3, from the PREREG): ④ ships `4·Ng` doubles against ②'s
`8·Ng`, so the intrinsic advantage is the **2× byte ratio**; the message-count term is an
implementation artifact, which is why ② is built both fused and unfused.

🔴 **Read L103 before writing the kernels**: do not put the selfcheck inside a kernel you will
time. That mistake cost this session a false +30 % and a wrong causal story.

</details>

### P3 — Reach the paper's core counts.
The ladder caps at **2304** because `MESHES_FESOM2.1/farc` has no larger partition. The 2019
paper's 6912 partitioning lives in `/pool/…/MESHES/fArc_sorted`, which **this port cannot run**:
immediate CG non-convergence at dt 900/300/180, with and without wsplit. That is **not** the
cold-start CFL class (which develops over tens of steps) — it fails at once, so suspect
configuration/mesh-format, not stability. Either fix that or partition
`MESHES_FESOM2.1/farc` to 4096+ with `/home/a/a270088/fesom_part/fesom2/work_part`.

### P4 — Unexplained residual.
Cell ③ costs +13 % `icedyn` on GPU at CORE2 np8. Measured: **not** kernel time (total kernel-busy
identical, 21.8 vs 21.7 ms/step; mEVP kernels marginally cheaper) and **not** the node kernel
(mask ablation 9.4 vs 9.5 ms). No mechanism claimed. If it matters, the next step is per-launch
gap analysis, not another hypothesis.

### P4b — VERIFY the farc GPU absolutes before publishing them.
`docs/SCALING_FARC.md` (2026-05-28) has farc GPU at 2.1225 s/step on 2 nodes; this campaign
measures 0.1272 — **~17×**. Candidates (none confirmed): M7 campaign ~8×, monthly IO disabled
(`jobs/io.config.m9_timing`), `PRINT_EVERY=999` suppressing the per-step `Gatherv`, and the
launch-bound effect. Decompose it — one run of the current binary with the OLD IO/print
settings would isolate most of it. Ratios between legs are unaffected and stand.

### P5 — Also unexplained.
Our farc ice share is ~13 % at 2304 ranks where the 2019 paper interpolates ~25–30 %. Candidates
(untested): the port's comms campaign (CGPIPE, fused halos) that FESOM2.0 lacked; mEVP here vs
standard EVP there.

## 5. Traps — read before running anything

1. 🔴 **On Serial every `FESOM_SPEED_*` lever is vetoed without `FESOM_SPEED_FORCE_SERIAL=1`.**
   15 levers shouted it in the first zero-point run, PHASESTATS included. Without that line the
   whole CPU half of a campaign returns 0.00 % differences. `job_m9_ab_cpu` sets it; if you
   write a new CPU job, set it and grep the announce. (L105a)
2. 🔴 **Never put a diagnostic branch in a kernel you intend to time.** On CUDA a never-taken
   branch costs the registers its body needs. This inflated cell ③ to +30 % GPU / +39 % CPU;
   moving it to its own launch gave +13 % / −0.65 %. (L103)
3. 🔴 **Quote the noise floor with every delta** — here ~0.2 %, from legs that must be identical
   (`classic` leg 1 vs `classic_noeps` leg 4). Rep 1 of leg 1 runs ~3 % slow from job warm-up;
   `min`-of-2 absorbs it, and leg 4 being as fast as leg 1 is what excludes leg-order drift.
4. 🔴 **Mesh root is `/pool/data/AWICM/FESOM2/MESHES_FESOM2.1/`** — as `jobs/*` already state for
   dars and ng5. Two hours went into `MESHES/fArc_sorted`, which has the same 638 387 nodes (so a
   node-count check passes) but a different partition set. (L105 corollary)
5. 🔴 **Freeze binaries to immutable `bin/b_<sha8>/`.** Overwriting a reused path failed with
   `Text file busy` while a job was executing it, and two gate jobs went out pinned to the wrong
   build. (L105b)
6. 🔴 **Ranks-per-node is a memory constraint on big meshes.** ng5_w3d at 64 ranks on ONE node
   OOMs; spread over ≥4. **Probe an unfamiliar mesh×ranks point before committing a fleet.**
   Both fleet failures this session were caught by probes. (L105c)
7. **`gpu_fidelity_check.py`'s PASS/FAIL is meaningless for a solver-class lever** — its ceilings
   are calibrated for CUDA-vs-Serial at *identical* arithmetic. Read the magnitudes.
   `job_m9_mevp_bitid CLASS=solver` prints a banner saying so.
8. **Quoting `icedyn` busy alone undercounts the ice cost ~3×.** Use ice+icedyn+iceadv, busy
   **and** wait, which is also the 2019 paper's definition. `TOTAL busy+wait` ≈ the measured
   s/step to ~1 %.

## 6. Open decisions

- **D1 resolved** (user): cell ② gets built both fused and unfused.
- **Push?** Branch is local. The project convention is that the user says when.
- **`FESOM_SPEED_MEVPLAG` adoption**: opt-in knob for now. Promotion needs P1.
- The `[m9] mEVP cell:` provenance line is the single grep target for every gate and A/B leg —
  keep it if you refactor.

---

## 7. Session-2 artefacts

| what | where |
|---|---|
| P1 pre-registration | `docs/plans/20260805-m9-PREREG-P1.md` (commit `c9a4d6a`, before the first leg) |
| P1 climate legs (9) | `/work/ab0995/a270088/port2/m9/clim/p1{c,g}_*` — CPU np256 + GPU np8, 1 yr each |
| P1 analysis | `scripts/m9_accuracy.py` (corr/RMS/bias + NH/SH area & extent vs the mEVP floor) |
| P1 job | `jobs/job_m9_climate1yr` (`BACKEND=cpu\|gpu`, one leg per job, BIN= required) |
| cells ②/④ | `src/fesom_ice_{evpwide.{h,cpp},maevp.cpp,.cpp}` |
| gate extension | `jobs/job_m9_mevp_bitid` gained `REFKNOBS=` (cell ④'s reference is MEVPDIV, not knob-off) |
| frozen bins | serial `bin/b_2138be66`, CUDA `bin/b_bfab84a2` — the ones every gate and timing above used |

**Gate job IDs of record.** Byte ladder 26711787 / 26711788 / 26712292 / 26711922 / 26712290 /
26712291; CUDA fidelity 26712516 / 26712517; options ×3 26712518. Timings (CLEAN, same
allocation per node count) 26712790 / 26712791 / 26712926. P1 legs 26711504-08 (CPU) and
26711512-15 (GPU); the 1500-step probe that established the determinism control and caught the
anchored-grep bug is 26711305-07.

**Two traps to add to §5's list:**

9. **`srun -l` prefixes every line with `<rank>: `, and the model's announces go to STDERR.**
   A `grep -E '^\[m9\]' run.log` therefore matches nothing and prints an EMPTY provenance
   block — which reads exactly like a clean run. Caught by the probe before the fleet.
10. **A floor quoted to 5 decimals must be COMPARED at 5 decimals.** Testing a computed
    correlation against a literal `1.00000` with `>=` fails everything that is not bitwise
    equal. The GPU control leg — same binary, same knobs — "failed" sst and ssh at corr
    1.000000. A bar a run fails against itself is a broken bar, and the control is what exposes it.
