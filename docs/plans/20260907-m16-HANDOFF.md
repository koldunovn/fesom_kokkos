# M16 handoff — 2026-09-07 evening (implementation day 1)

**Where:** worktree `~/port_kokkos_sp`, branch `m16-precision` (off `m14-integrate d4a9fe0`).
Plan `docs/plans/20260902-m16-mixed-precision.md` (ticks are current), registry
`docs/PRECISION_ISLANDS.md`, this file. Nothing is pushed. **Standing rule paid today: no model
run on the login node except seconds-long pi smokes — CORE2 = SLURM (`jobs/job_m16_gate_serial`).**

## Commits on `m16-precision` (all local)
| sha | what | gate status |
|---|---|---|
| `ac8c0d7` | P2 worktree, upstream diffs, `build_m16.sh`, `ref0` oracle, `scripts/m16_gate0.sh` | i4 ≡ ref0 on pi np1/np2 |
| `4d2768a` | M15 knob cherry-pick (P1 = `39d7a30` on `m15-precond`) | — |
| `4f3e84e` | P3 PRECOND default 1, announce + defect ratio, SYMPRE rule, pcsi on 1/2/3 | pi bitwise (PRECOND=0); CORE2 np1 CG 133→81 iters (−39 %) |
| `d6c907d` | A1 `USE_SINGLE_PRECISION`, `real_t`/`dbl_t`/`FESOM_MPI_REAL`, banner | pi bitwise; SP configure 54/54 TUs |
| `7830442` | A2 conformance table + `scripts/m16_accum_ledger.py` | — |
| `4d11638` | **B1–B7, B8 invariant, C1 SP Serial build** (WIP-of-record, one commit — the slices overlap in the I/O files) | pi np1+np2 bitwise for b12/b3/b6/b7; restart round-trip bitwise; **CORE2 np8 SLURM gates were RUNNING at commit** |

## What was running when the session ended — READ THESE FIRST next time
Logs: `/work/ab0995/a270088/port2/m16/logs/gate.<job>.out`. Each job prints one line per config
(`BYTE-IDENTICAL` / `DIFFERS` / `RUN rc=`) and a final `GATE 0 PASS|FAIL`.

| job | binary (frozen, md5 in `…/m16/bin/<tag>/MD5.txt`) | content | state at logout |
|---|---|---|---|
| 27286999 | `bin/b12` (`7f1ce202`) | B1+B2 | running, 6/14 identical, 0 bad |
| 27287001 | `bin/b6` (`4182a4cb`) | B1–B6c | running, 6/14 identical, 0 bad |
| 27287827 | `bin/b3` (`144410db`) | B1–B3 + `verify` leg | just started |
| 27287828 | `bin/b7` (`5440d4e9`) | all of B + `verify` | just started |

If all four say `GATE 0 PASS`: Phase B is byte-certified on CORE2 at 8 ranks — tick the four
"CORE2 SLURM pending" lines in the plan (B2, B3, B4/B5, B6a–B7 gate bullets) and move on.
If one DIFFERS: the config name tells the slice (solver configs → B3; `se`/`sewide` → B4;
`evpwlean` → B5; `det`/`tke` → B6a/B7; everything → B2 halo/io). Bisect by rebuilding from the
commits `d6c907d` (pre-B) and the b12/b3/b6 file lists in the commit message.

Oracle: `gate0_core2/ref0/<config>_np8/` (14 configs, from the unmodified `d4a9fe0` build
`build-m16-ref0`); pi oracle `gate0/ref0/<config>_np{1,2}/` (14 configs incl. `cgpipe`/`cgpoly`).

## Next steps, in order
1. **Read the four gate logs** (above). Then run `scripts/m16_gate0.sh build-m16serial all 1` on
   pi after any rebuild — seconds — before anything else.
2. **C2, SP CUDA build:** `build_m16.sh cuda` (DP) built fine (`build-m16cuda/fesom_port`
   `ba6334ad`, links openmpi 4.1.5-nvhpc — the RPATH rule holds). `build_m16.sh cuda-sp` fails
   with ONE error: `src/fesom_halo_device.cpp:706` — the host-vs-device verification builds
   `std::vector<double> ref(...)` from `real_t` and passes `double*` to a `real_t*` parameter;
   make that vector `real_t` (it is a diagnostic; keep the `maxd` double). Rebuild, `ldd`, then
   the CUDA pi smoke needs a GPU allocation (`srun -p gpu-devel -A ab0995 --gres=gpu:1`, or a
   job) — never on the login node.
3. **B3b, the stiffness shadow** (SP-only `values_full` `FieldT<dbl_t>`, seeded on the FIRST
   `fesom_update_stiff_mat_ale_kk` call, atomic_add in dbl_t, `values = real_t(shadow)` per update,
   `FESOM_DIAG_STIFF_DRIFT=1` twin print, SP restart writes the shadow into `stiff_values`). Code
   sites: `src/fesom_ssh.cpp` `fesom_update_stiff_mat_ale_kk` (~line 2140), `fesom_ssh.h` struct,
   `fesom_io_restart.cpp` stiff write/read (`vid_sval`, ~660/~840). July's version is in the
   B3 checklist (`git -C ~/port_kokkos_mp diff 1df683b m8-precision -- src/fesom_ssh.cpp`,
   `values_dbl_fld`) — upstream's semantics differ only in WHERE it is seeded. Gate: DP bitwise
   by construction (compiled out); SP pi zstar with the drift print non-zero and bounded.
4. **Phase D, salt anomaly** — the checklist is `docs/reference/upstream_sp/pr986.diff` (every
   `S_ref_anomaly` hunk) and the port twins found today: EOS `s1 = S[...]` at `fesom_eos.cpp:896`
   (host) / `:966` (device) plus `densityJM_components` callers; `sw_alpha_beta` (`:880-1006`);
   ice coupling `rsss` `fesom_ice_coupling.cpp:272-277` (host) / `:353-376` (device),
   `relax_salt` `:290`, `S_oc` at `:65`; tracer_diff surface BC `:77-83` (host) / `:383-394`
   (device functor); KPP buoyancy flux `fesom_kpp.cpp:1395` / `:1587`; `fesom_tracers.h` has
   `values/valuesAB/valuesold`; PHC `insitu2pot` at `fesom_phc.cpp:773` (convert AFTER it);
   io streams `salt`/`sss` at `fesom_io.cpp:1126/1129` (add a per-var offset applied at
   accumulation, host + device resolvers); restart detection in `fesom_io_restart.cpp` read
   path; step-diag S bounds in `fesom_main.cpp` (grep `S_min`). Gate D2 = S_ref invariance on
   pi (off / 35 / 10 agree to rounding) — the one that finds a missed consumer.
5. Phase E (harness `PREC=` axis, Gates 2–4) per the plan; all SLURM.

## Facts learned today (not in the code)
- The unmodified base `d4a9fe0` segfaults in `fesom_jra55_step()` with the EVPWIDE-lean arm on
  CORE2 at **np2** (extended zone built, then crash); np8 and pi np2 fine. Base finding, noted in
  the plan before Phase B.
- SP vs DP on pi (default, from rest): after ONE step |Δu| up to 21 % of max|u| at a few slots
  (absolute ≈ 1e-4 m/s), T/S at rounding level. Suspect: float `hpressure` running sum → spurious
  PGF at tiny velocities (registry "promote FIRST"). Not acted on; first candidate if G3/G4 object.
- CORE2 symmetry-defect ratio of the as-built preconditioner is 0.638 (= M10 finding F1);
  variants 1/2/3 are 4e-14. pcsi-vs-cg differences in Av/Kv are mixed-layer threshold flips
  (identical under cg2-vs-cg), not a solver property.
- The SP compile seam map after the sweep was tiny (io_restart nc_*_double, evpwide assert,
  dump assert, two unit tests) — the sweep was near-complete on the first pass.
- Process: `build_m16.sh` deletes the binary before building (a stale binary once passed a
  gate); never edit a running bash script (replace atomically with `mv`); `pkill -f` kills the
  calling shell if the pattern appears in its own command line — kill by pid list instead;
  pass CONFIGS="all verify" (the list expander is fixed).

## Tools written today
`scripts/m16_gate0.sh` (pi/core2 presets, `all|five|<list>`, `verify` alias, absolute-binary
mode), `scripts/m16_p3_gate.sh`, `scripts/m16_restart_gate.sh`, `scripts/m16_accum_ledger.py`,
`scripts/m16_mpi_invariant.sh`, `scripts/mp_assert_banner.sh`, `jobs/job_m16_gate_serial`,
`build_m16.sh` (ref0|serial|serial-sp|cuda|cuda-sp).
