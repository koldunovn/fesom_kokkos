# M16 handoff — 2026-09-09 (end of day 4) — START HERE

**Where:** worktree `~/port_kokkos_sp`, branch `m16-precision` (off `m14-integrate d4a9fe0`). `origin` is
still at `f91aa86`; **today's four commits (`de8069b`, `d67ab49`, `af79a84`, this one) are LOCAL — ask
before pushing.** Plan `docs/plans/20260902-m16-mixed-precision.md` (decisions D1–**D13**), board
`docs/MIXED_PRECISION_M16.md`, registry `docs/PRECISION_ISLANDS.md`. Day-3 map:
`20260908-m16-HANDOFF.md` — still correct on the allocator, the `e3` pair and gates 0–3.

## The two things that changed today

🔴 **1. The purpose was re-stated by the user: this track is the PORT plus its FAITHFULNESS against the
Fortran mixed-precision oracle (upstream #940, Suvarchal's PR). It is not a scaling campaign.**
Scaling *is* owed, but later, and in a specific shape (**plan D13**): CPU **and** GPU strong scaling of
**SP alone**, measured **without** the three M14 optimisations — ice (M9), solver (M10), partitioning
(M11) — because those are the *next* paper, on top of SP. Gate G2 had drifted into a small scaling
study: six 16-node GPU jobs sat in the queue for 20 h to answer a question that needed two points.
Unconfirmed at the scope boundary, ask before the campaign: split-explicit SSH (M12/M12b) and the #984
preconditioner were not named among the three; #984 is upstream's shipped default so it reads as
baseline, split-explicit is read as baseline-eligible.

🔴 **2. The oracle never existed on this machine, and now it does.** `~/FESOM_port/fesom2` is at PR
**#867**, predating #940's merge (2026-08-28); no other Fortran checkout here compiles
`USE_SINGLE_PRECISION`. So G4's bar ("SP-vs-Fortran ≡ DP-vs-Fortran to the printed digit") had nothing
on the other side. Built today from `~/fesom2_sp` @ **`a62f180`** (upstream main; contains #940 + #984
#986 #995 #997), Intel, both precisions:
```
/work/ab0995/a270088/port2/m16/oracle/{dp,sp}/bin/fesom.x   (+ PROVENANCE.txt, md5s)
/work/ab0995/a270088/port2/m16/oracle/build_{dp,sp}/        (BUILD_TESTING=ON)
```
Verified by the CMake caches (`USE_SINGLE_PRECISION` OFF / ON) and by **job 27340546**: upstream's own
local ctest suite, **13/13 PASS in both precisions** on Intel/Levante (upstream only runs it with GNU on
ubuntu — this is its first Intel run). ⚠️ **Read what that pass means:** the criterion is exit code +
success markers + no failure signature + the output file exists — a robustness check, **not** a numeric
one. Its one hard precision fact: in an SP build the harness makes the `SINGLE PRECISION MODE` banner a
mandatory marker, so 13/13 proves the binary really runs single precision. Rebuild/rerun:
`scripts/…` → `bash /scratch/…/build_oracle_tests.sh` is gone with the scratchpad; the recipe is
`cd ~/fesom2_sp && source env.sh levante.dkrz.de+intel`, then cmake with
`-DCMAKE_BUILD_TYPE=Release -DUSE_SINGLE_PRECISION=ON|OFF -DBUILD_TESTING=ON -DENABLE_MPI_TESTS=ON
-DBUILD_MESHDIAG=ON -DBUILD_MESHPARTITIONER=ON -DTEST_TIMEOUT=600`, and `jobs/job_m16_oracle_ctest`.

## 🔴 Suvarchal's own SP experiment set — READ `reference-suvi-sp-runs` (memory) FIRST
`/work/ab0995/b380667/fesom_exps_19082026`, owner **b380667 = Suvarchal Kumar Cheedela** (uid 24921,
verified by ownership, not by a comment), 19–21 Aug 2026, readable. Found via
`~/presentations/fesom_conversion/build/paper_sp/reduce_suvi60.py`; paper repo `~/Suvi/paper_sp`.

| experiment | arms |
|---|---|
| `core2_60yr` | `dp_n512` / `sp_n512`, 1948–2007, absolute salinity |
| `core2_60yr_saltanom` | `dp_n512` / `sp_n512`, `use_salt_anomaly=.true.` |
| `core2_60yr_dpnoise_s12345` / `_s22318` | `dp_n864`, FP64 + 0.0002 K noise at step 1 |
| **`core2_scaling`** | **dp AND sp at 64/72/128/144/256/288/432/512/864 ranks** |
| `core2_scaling_dp_p1` | dp only, same ladder |
| `pi_10yr(_saltanom)`, `pi_10yr_dpnoise_*` (4 seeds), `pi_amp_a0p002_*` | 8-rank pi twins + ensembles |

**Two consequences.** (a) The **noise pairs are the significance bar** — an SP−DP difference only counts
if it exceeds the FP64 seed spread. Mirror that design; do not compare SP against DP in isolation.
(b) `core2_scaling` is most of what paper 1 owes **on the Fortran side** (plain upstream, none of the
three levers) — what is missing is the *port's* counterpart.

**His config** (`core2_60yr_saltanom/sp_n512/namelist.*`): **/pool** CORE2 mesh
(`MESHES_FESOM2.1/core2`, not our private copy), **CORE2 forcing** (`FORCING/CORE2/`, daily,
`nm_nc_freq=1`), `zstar`, `step_per_day=96` ⇒ **dt = 900 s**, start 1948, yearly restarts, KPP.
Binary: branch `sp/integration-60y` @ **`27ed9f4f`** in `/work/ab0995/b380667/fesom2_19082026` —
**pre-merge**, so his output is a *physics* reference, not a bit-level one, unless his commit is also
built (his tree is readable). Compiler `/sw/spack-levante/openmpi-4.1.2-yfwe6t/bin/mpif90` =
**ifort 2021.5.0**, Release — the **same wrapper our oracle build picked up**.

🔴 **The forcing gap.** The port reads **JRA55-do only** (`src/fesom_jra55.cpp:667`, one static table of
8 names used as BOTH file prefix and variable name). CORE2 forcing breaks that: `ncar_rad.` carries
`SWDN_MOD`+`LWDN_MOD` and `ncar_precip.` carries `RAIN`+`SNOW`, and no name matches. Needed: a
per-dataset table separating file prefix from variable name, plus a configurable time origin. Bounded,
not architectural — but it is on the forcing path, so it re-opens gate G0. *(Estimate from the init code;
the time-axis handling was NOT audited.)* Until then the setup **both codes can share** is upstream's own
`setups/test_core2` = CORE2 mesh + **JRA55** + PHC, which is exactly what the port runs.

**Upstream ships numeric truth values** (16 digits) in `setups/*/setup.yml` `fcheck:` blocks —
`test_core2`: a_ice 0.24199584126472473, sst 11.215014457702637, salt 21.45014190673828,
temp 4.137086868286133; `test_pi`: a_ice 0.2738901286952792, sst 8.677108944419707,
salt 34.730307510851034, temp 2.47356667437993, u −0.002011676818348277, v 0.0002909514166182032.
These are DP references. **The sharp faithfulness statement:** measure how far Fortran-SP departs from
them and how far port-SP departs from them, and require the two departures to agree. `update_truth_values.sh`
regenerates them.

## Gate state (changes from day 3 only)
| gate | status |
|---|---|
| G0 / G1 / G3 | unchanged — closed (day-3 handoff) |
| G2 | **CPU rows final. GPU rows now at the PRODUCTION posture, not a ladder:** CORE2 1N 0.870; **2N knobs-off 0.893 (27339007), 2N recipe 0.889 (27339008)**, leg spread 0.00–0.46 %, 3 min each. **Device memory now measured** — the ladder polls `nvidia-smi` per leg and prints `gpumem_max=`: **2635→1537 MiB = 0.583×** (July's hand-sampled "0.51×" was optimistic; the FP64 islands and the CUDA context do not halve). NG5 4N pair **27339009 still queued** (est. 17:19). The four 16N rows were DROPPED (D13): CORE2 is past its knee at 16 nodes on Levante (0.0618 s/step at 1N vs 0.0794 at 16N). |
| G4 | unchanged: CPU screens PASS; GPU screens on `e3` not run; 1-yr twin not started — **but its design should now follow Suvi's**: SP vs DP judged against the FP64 noise-seed envelope. |

## Do first next session
1. 🔴 **`scancel 27310270 27313650 27313843 27313844 27313845 27313846`** — six superseded 16-node GPU
   jobs, still queued, still delaying 27339009. `scancel` is blocked for the agent by the permission
   classifier; the user runs it (`! scancel …`). All are PENDING, so no restart chain is at risk.
2. Read **27339009** (`gladder.27339009.out`) → board §1b NG5 4N row + its `gpumem_max=` (the first
   large-mesh SP memory number).
3. **Decide the comparison matrix and run it** on `setups/test_core2` (CORE2 mesh + JRA55 + PHC, the
   only setup both codes share): Fortran-dp, Fortran-sp, port-dp, port-sp, plus a **noise pair** for the
   envelope. Bar = the two SP−DP departures agree, measured against the `fcheck:` DP truth values.
4. Ask the user: is a **CORE2-forcing reader** in the port worth the G0 re-gate, to reach Suvi's exact
   60-yr setup? If yes it is the cheapest route to a like-for-like 60-yr comparison against runs that
   already exist.
5. Then G4 GPU screens on `e3` and the 1-yr twin — the fidelity work that actually gates "SP is usable".

## Rules re-paid today (on top of the day-3 list)
- **A queue estimate is not a schedule.** The six 16-node jobs' start slipped ~21 h overnight. A 2-node
  job ran 3 min after submission; the compute partition served a test job in <2 min. Prefer the small
  posture unless scale IS the question.
- **Check the machine before quoting a curve.** The M14 scaling CSV is **JUPITER GH200**, not Levante —
  those job ids are not in Levante's sacct, and it goes to 256 nodes, which the 16-node cap forbids.
- **Verify provenance by ownership, not by a comment** (the user asked: "make sure it's not mine").
- `scancel`, and compound commands containing a foreground `sleep`, are blocked for the agent.
- Sourcing FESOM2's `env.sh` under `set -u` dies on an unbound `LD_LIBRARY_PATH`.

## Tools added today
`jobs/job_m16_oracle_ctest` (upstream's ctest suite on both oracle builds, compute partition) ·
`jobs/job_m14_ladder_gpu` now prints `gpumem_max=<MiB>` per leg (10 s `nvidia-smi` poll on the batch
node; ~20 ms per sample against a ~2 % leg spread) · `~/fesom2_sp` (upstream main clone) ·
`/work/ab0995/a270088/port2/m16/oracle/` (the oracle, both precisions, with provenance).
