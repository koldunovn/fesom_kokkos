# M16 handoff — 2026-09-09 (end of day 5) — START HERE

**Where:** worktree `~/port_kokkos_sp`, branch `m16-precision`. `origin` is still at `f91aa86`;
**everything since is LOCAL — ask before pushing** (day-4's four commits plus day-5's; `git log
--oneline f91aa86..HEAD`). Plan `docs/plans/20260902-m16-mixed-precision.md` (decisions D1–**D14**),
board `docs/MIXED_PRECISION_M16.md` (new **§3b**), registry `docs/PRECISION_ISLANDS.md`. Day-4 map:
`20260909-m16-HANDOFF.md` — still correct on the oracle, Suvarchal's runs, and the forcing gap.

## What day 5 did

**G4 stopped being a bar with nothing behind it.** Day 4 built the Fortran oracle; day 5 built the
comparison it exists for, ran it, and read it. Also: the six "superseded" 16-node GPU jobs day 4 said
to cancel had already run, so their rows are on the board for free.

## 1. The faithfulness matrix (plan D14, board §3b) — BUILT and READ at 1 month

Four arms on the one setup both codes share, upstream's `setups/test_core2` (CORE2 mesh + JRA55 +
PHC, 1958), plus FP64 noise twins from upstream's own `&oce_perturb`:

- `scripts/m16_faith_setup.sh <rundir> [len] [unit] [seed] [step_per_day] [amp] [det]` — builds a
  Fortran run dir. **Read its banner before changing anything**: it argues the four deliberate
  deviations from upstream's `setup.yml`, each forced by "both codes must run the same physics".
- `jobs/job_m16_faith_fortran` (oracle, either precision; asserts the SP banner and that a requested
  perturbation FIRED) and `jobs/job_m16_faith_port` (Kokkos-Serial `e3` bins).
- `scripts/m16_faith_compare.py <runroot> [--vars …] [--rec N] [--where]` — the verdict.

**Verified shared, not assumed:** identical PHC file (byte-identical), identical `PHC2_salx.nc` and
`CORE2_runoff.nc`, identical output window (one record, shape (1,47,126858), time 1339200 s).

### The four results (Jan 1958, 64 ranks / 1 node, dt 2700; jobs 27355327 / 27355390 / 27355632)

1. **RATIO port/Fortran of the SP−DP departure = 0.94–1.54** (a_ice 0.94, temp 1.29, sst 1.38, salt
   1.54). That is G4's statement, first quantified, and it needs neither departure to be small.
2. 🔴 **The FP64 noise envelope SATURATES by one month.** σ=1e-6 K and σ=2e-4 K are **200× apart in
   amplitude and under 20 % apart in envelope** — so it is the model's own noise floor, not a property
   of the nudge, which makes it a real bar. Every SP−DP departure sits **1.1–2.8× ABOVE it**.
   **So "SP is buried in the noise" is FALSE at one month** and can only be claimed at climate length.
   *(This is the opposite of what was predicted. A perturbed IC is one kick that then grows at the
   flow's own rate; SP injects rounding at every operation of every step.)*
3. **SP carries a small SYSTEMATIC component and both codes carry it** — sst mean shift vs own DP
   −1.53e-05 (Fortran) / −2.84e-05 (port), against −3.3e-07 / −9.8e-07 for the noise twins. The port
   reproduces upstream's SP *bias*, which is stronger than the ratio alone.
4. **The codes differ from each other, at equal precision, 20–30× more than either differs from its own
   DP arm** (sst 3.49e-03). Characterised: **not the ice edge** (44.7 % of the sum of squares in the top
   1 % of nodes, median latitude −36, ice edge *under*-represented 0.04 vs 0.14 — not the M9 class), and
   **not the IC hole fill** — `ic_extrap_det` on in both codes moves it 0.4/2.9/0.8 % (sst/a_ice/temp).
   **Scale anchor:** that supported option moves each code's own solution *more* (4.86e-03 / 4.94e-03)
   than the codes differ from each other. Run the matrix with **det OFF**, the shared default.

## 2. G2 board: the dropped 16-node rows ran anyway
`27313843/44/45` reached the queue front before cancellation. CORE2 decays **0.870 (1N) → 0.893 (2N) →
0.941 (16N)**; **NG5 does not move: 0.863 at 4N and 0.863 at 16N**. The SP prize tracks the **byte share
of the step**, not the node count. NG5 4N device memory **36765 → 20129 MiB = 0.547×** (CORE2 0.583×).
`27313844` (CORE2 16N recipe) is **withdrawn** — its SP legs spread 2.95 %, wider than the 1.2 % gap.
🔴 The 16N halo pairs' printed `GAIN` is **wrong** (submitted before the env-reset fix `f91aa86` was
committed the same afternoon; leg 4 inherited `FESOM_HALO_STAGE=1` and min-over-legs picked it).
Clean legs only: staged beats device by **35–47 %** at 16 nodes, up from 24 % at 4.

## Do first next session
1. 🔴 **`scancel 27313846`** — the last superseded 16-node GPU job, still PENDING (it measures the M14
   recipe, which D13 puts outside the SP paper). `scancel` is blocked for the agent; the user runs it.
2. **Read the 1-year matrix** — in flight at submission time, dt 1800 (protocol), 8 × 1 node:
   `27355667` fdp · `27355668` fsp · `27355669/70` fdp_r{12345,22318} (σ 1e-6 K) · `27355671/72`
   fdp_s{12345,22318} (σ 2e-4 K) · `27355673` pdp · `27355675` psp, all under
   `/work/ab0995/a270088/port2/m16/faith/year_1958`. Then
   `scripts/m16_faith_compare.py .../year_1958 --where`, and per month with `--rec 0..11`: **the month
   at which the envelope overtakes SP−DP is the headline number of this gate.**
3. **Then the GPU side of G4** — the screens on `e3` and a GPU arm of the matrix. Note CUDA is not
   run-to-run reproducible, so a GPU arm needs its own self-noise envelope, not the FP64 one.
4. **Still open for the user (day 4, unanswered):** is a **CORE2-forcing reader** in the port worth the
   G0 re-gate? It is the only route to Suvarchal's exact 60-yr setup, whose runs already exist. The
   port reads JRA55 only — one static table of 8 names used as both file prefix and variable name
   (`src/fesom_jra55.cpp:667`); CORE2 breaks that (`ncar_rad.` carries `SWDN_MOD`+`LWDN_MOD`).

## Rules re-paid today
- 🔴 **A silent knob needs an empirical fired-check.** Fortran's `ic_extrap_det` prints *nothing*
  (`gen_support.F90` calls `extrap_nod3D_det` and returns). Proof it fired = det-vs-legacy *within* a
  code. The port announces its equivalent; upstream does not. (L80, the hard way.)
- 🔴 **SLURM snapshots the batch script at SUBMIT.** Two 16-node jobs ran a script whose bug was fixed
  two hours after they were queued, and the fix in git said nothing about what was actually running.
  Check submit time against the fix commit before trusting an overnight result.
- **min-over-legs rewards a contaminated leg.** A leg that inherits the other arm's knob is always the
  fast one, so it always wins the min. Third occurrence. The zombie check rejects *dead* legs, not
  *mislabelled* ones.
- **A comparison is only as shared as its inputs.** `/pool`'s CORE2 `nlvls.out`/`elvls.out` differ from
  our private copy at 2 nodes and 4 elements (rewritten 2026-07-03). Both codes must read one file.
- **Ask a precision question away from a stability limit.** dt 2700 (upstream's CI value) logged
  CFLz_max 2.46 against a 1.75 threshold on a cold start; production uses dt 1800.
- Compound commands containing a foreground `sleep` are blocked for the agent (use a background wait).

## Tools added today
`scripts/m16_faith_setup.sh` · `jobs/job_m16_faith_{fortran,port}` · `scripts/m16_faith_compare.py`
(with `--where`) · run trees `/work/ab0995/a270088/port2/m16/faith/{pilot_1m,pilot_1m_det,year_1958}`.
